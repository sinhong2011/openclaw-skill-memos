{
  "name": "openclaw-memos-mcp",
  "version": "0.1.5",
  "description": "MCP server for full CRUD operations on a Memos instance",
  "type": "module",
  "bin": {
    "openclaw-memos-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc && node -e \"const f='dist/index.js';const c=require('fs').readFileSync(f,'utf8');if(!c.startsWith('#!'))require('fs').writeFileSync(f,'#!/usr/bin/env node\\n'+c);require('fs').chmodSync(f,0o755)\"",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "mcp",
    "memos",
    "usememos",
    "notes",
    "openclaw"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sinhong2011/openclaw-skill-memos.git",
    "directory": "mcp"
  },
  "bugs": {
    "url": "https://github.com/sinhong2011/openclaw-skill-memos/issues"
  },
  "homepage": "https://github.com/sinhong2011/openclaw-skill-memos#readme",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "dotenv": "^16.4.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
